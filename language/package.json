{
  "name": "@daemon/language",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./schema-validator": "./dist/data/schema-language/validator.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test --import tsx data/schema-language/validator.test.ts",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
