# Ontology fixtures

Sample object payloads for tests and documentation. Not loaded at runtime unless copied into seed or tests.

| File | Purpose |
|------|---------|
| `sample-signal.json` | Open high-severity signal |
| `sample-case.json` | Case linked to a signal |

Regenerate from seed output when the core schema changes.
