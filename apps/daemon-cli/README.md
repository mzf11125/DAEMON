# DAEMON CLI (Merge Phase 1)

Placeholder for vendored CLI from upstream `apps/cli` → `apps/daemon-cli`.

No breaking changes to existing `make` / `scripts/` workflows until the CLI is pinned in `external/daemon-system-ontology/`.
