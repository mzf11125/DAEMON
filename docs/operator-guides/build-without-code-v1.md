# Build without code v1 (operators)

What operators can do **without** engineering in v1.

| Capability | v1 | Post–v1 |
|------------|-----|---------|
| Open case from signal | Yes (console) | |
| Record decision | Yes | |
| Read audit | Yes | |
| Change ontology types | No — PR + eng | Pack config UI |
| Add rules | No — PR | Rule studio |
| Enable sector pack | Env/query (limited) | Tenant admin |

Configuration changes to ontology semantics require engineering review and assumption register update (A-NONTECH-02).
