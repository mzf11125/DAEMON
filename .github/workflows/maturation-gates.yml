name: Maturation gates

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  policy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Doc policy (no premature production-agent claims)
        run: ./scripts/check-maturation-policy.sh
