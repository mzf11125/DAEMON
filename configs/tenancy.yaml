tenants:
  - id: default
    displayName: Default
    kind: platform
    enabledDomains:
      - foundation
    quotas:
      maxEntities: 1000000
      maxWritesPerMinute: 5000
  - id: inst-alpha
    displayName: Institution Alpha
    kind: institution
    enabledDomains:
      - foundation
    quotas:
      maxEntities: 500000
      maxWritesPerMinute: 2000
  - id: ent-beta
    displayName: Enterprise Beta
    kind: enterprise
    enabledDomains:
      - foundation
    quotas:
      maxEntities: 250000
      maxWritesPerMinute: 1000
  - id: logistics-pilot
    displayName: Logistics pilot
    kind: institution
    enabledDomains:
      - foundation
      - logistics
    quotas:
      maxEntities: 100000
      maxWritesPerMinute: 500
  - id: abc-antero
    displayName: ABC Antero lens
    kind: institution
    enabledDomains:
      - foundation
      - logistics
    quotas:
      maxEntities: 200000
      maxWritesPerMinute: 800
  - id: abc-arandy
    displayName: ABC Arandy lens
    kind: institution
    enabledDomains:
      - foundation
      - logistics
    quotas:
      maxEntities: 200000
      maxWritesPerMinute: 800
  - id: abc-holding
    displayName: ABC Express Group holding
    kind: platform
    enabledDomains:
      - foundation
      - logistics
    quotas:
      maxEntities: 500000
      maxWritesPerMinute: 2000
