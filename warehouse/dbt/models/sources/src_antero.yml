version: 2

sources:
  - name: raw_antero
    schema: raw_antero
    freshness:
      warn_after: { count: 30, period: minute }
      error_after: { count: 60, period: minute }
    tables:
      - name: pos
        identifier: pos
      - name: manifest
        identifier: manifest
      - name: city
        identifier: city
      - name: kantor_coverage_kota
        identifier: kantor_coverage_kota
