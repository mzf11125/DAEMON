apiVersion: 1

datasources:
  - name: DAEMON Ontology
    type: daemon-ontology-datasource
    access: proxy
    url: http://localhost:8081
    isDefault: false
    jsonData:
      tenantId: tenant-demo
    secureJsonData:
      authToken: ""
    editable: true
