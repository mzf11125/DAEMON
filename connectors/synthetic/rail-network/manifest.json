{
  "connectorId": "synthetic-rail-network",
  "displayName": "Synthetic sandbox — rail-network",
  "description": "Developer-local fixture connector; replay-only CSV/JSON under connectors/synthetic/rail-network/",
  "version": "1.0.0",
  "paramsSchema": {
    "type": "object",
    "properties": {
      "fixturePath": { "type": "string", "default": "fixtures/sample.json" }
    }
  }
}
