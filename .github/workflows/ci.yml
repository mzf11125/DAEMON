name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
<<<<<<< HEAD
        with:
          submodules: recursive
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.10"
=======
>>>>>>> upstream/main
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm
<<<<<<< HEAD
      - run: go work sync || true
      - run: |
          for dir in packages/go-common packages/pipeline-runner infra/seed; do
            (cd "$dir" && go mod tidy && go vet ./... && go test ./...) || exit 1
          done
          for dir in services/platform-api services/ontology-service services/ingestion-service \
            services/rules-engine services/case-service \
            pipelines/raw-ingest pipelines/transforms pipelines/features pipelines/quality \
            pipelines/propensity-train pipelines/audit-archival; do
            (cd "$dir" && go mod tidy && go vet ./... && go build -o /dev/null ./cmd && go test ./...) || exit 1
          done
      - run: pnpm install --frozen-lockfile || pnpm install
      - run: make ontology-sync
      - run: make ontology-validate
      - run: ./scripts/check-no-stub-handlers.sh
      - run: ./scripts/check-maturation-policy.sh
      - run: ./scripts/check-vendor-neutral-language.sh
      - run: ./scripts/check-sandbox-registry-drift.sh
      - run: ./scripts/check-express-cargo-catalog.sh
      - run: pnpm -r typecheck
      - run: pnpm --filter @daemon/ontology-language test
      - run: pnpm --filter @daemon/cli test || echo "cli test skipped if DB unavailable"
      - run: pnpm --filter @daemon/ontology-functions test
      - run: pnpm --filter @daemon/agent-service run lint
=======
      - run: pnpm install --frozen-lockfile
      - run: pnpm run spec:check
      - run: pnpm run check:architecture
      - run: pnpm run check:route-auth
      - run: pnpm run check:tenancy-config
      - run: pnpm run check:ontology-pack
      - run: pnpm run check:governance-policies
      - run: pnpm run build
      - run: pnpm run test
      - run: pnpm run test:repo
>>>>>>> upstream/main

  integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.10"
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: pnpm install --frozen-lockfile || pnpm install
      - run: make ontology-sync
      - run: go work sync
      - run: make test-integration
        env:
          INTEGRATION_WITH_NEO4J: "1"

  e2e-full:
    runs-on: ubuntu-latest
    needs: [validate]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.10"
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: pnpm install --frozen-lockfile || pnpm install
      - run: go work sync
      - run: make up
      - run: make supabase-up
      - run: make migrate
      - run: make ontology-sync
      - run: |
          eval "$(supabase status -o env | tr -d '"')" || true
          export NEXT_PUBLIC_SUPABASE_ANON_KEY="${ANON_KEY:-}"
          export SUPABASE_URL="${API_URL:-http://127.0.0.1:54331}"
          export E2E_FULL=1
          ./scripts/e2e-smoke.sh

  supply-chain:
    runs-on: ubuntu-latest
    needs: [validate]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
<<<<<<< HEAD
          go-version: "1.25.10"
=======
          go-version: "1.26.3"
      - run: go work sync
      - run: go test ./collect-sensing/... ./security-governance/authn/... ./toolchain/sdk/go/... -count=1

  rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cargo test --workspace

  integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: daemon
          POSTGRES_PASSWORD: daemon
          POSTGRES_DB: daemon
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U daemon"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
>>>>>>> upstream/main
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm
<<<<<<< HEAD
      - run: go install golang.org/x/vuln/cmd/govulncheck@latest
      - run: |
          # testutil pulls testcontainers/docker; scan production packages only in go-common
          (cd packages/go-common && govulncheck ./auth/... ./config/... ./ctxkeys/... ./db/... ./http/... ./rules/...) || exit 1
          for dir in services/platform-api services/ontology-service \
            services/ingestion-service services/rules-engine services/case-service \
            pipelines/raw-ingest pipelines/transforms pipelines/features pipelines/quality infra/seed; do
            (cd "$dir" && govulncheck ./...) || exit 1
          done
      - run: pnpm install --frozen-lockfile || pnpm install
      - run: pnpm audit --audit-level=high
        continue-on-error: true
=======
      - run: pnpm install --frozen-lockfile
      - run: pnpm run build
      - name: Apply database migrations
        env:
          DAEMON_POSTGRES_URL: postgresql://daemon:daemon@localhost:5432/daemon
        run: pnpm run db:migrate
      - name: Repo integration tests
        env:
          DAEMON_API_KEY: daemon-dev-key
          DAEMON_POSTGRES_URL: postgresql://daemon_app:daemon_app@localhost:5432/daemon
          DAEMON_REDIS_URL: redis://localhost:6379
          DAEMON_INTEGRATION_REQUIRED: "1"
        run: pnpm run test:repo

  parity-golden:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: daemon
          POSTGRES_PASSWORD: daemon
          POSTGRES_DB: daemon
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U daemon"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run build
      - run: pnpm run check:parity-matrix
      - name: Apply database migrations
        env:
          DAEMON_POSTGRES_URL: postgresql://daemon:daemon@localhost:5432/daemon
        run: pnpm run db:migrate
      - name: Foundry parity golden E2E
        env:
          DAEMON_POSTGRES_URL: postgresql://daemon_app:daemon_app@localhost:5432/daemon
          DAEMON_INTEGRATION_REQUIRED: "1"
          DAEMON_REPO_ROOT: ${{ github.workspace }}
        run: node --test --import tsx tests/integration/foundry-parity-golden.integration.test.ts

  antero-parity:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: daemon
          POSTGRES_PASSWORD: daemon
          POSTGRES_DB: daemon
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U daemon"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run build
      - name: Apply database migrations
        env:
          DAEMON_POSTGRES_URL: postgresql://daemon:daemon@localhost:5432/daemon
        run: pnpm run db:migrate
      - name: ANTERO shadow parity golden E2E
        env:
          DAEMON_POSTGRES_URL: postgresql://daemon_app:daemon_app@localhost:5432/daemon
          DAEMON_INTEGRATION_REQUIRED: "1"
          DAEMON_ABC_FIXTURES: "1"
          DAEMON_REPO_ROOT: ${{ github.workspace }}
        run: node --test --import tsx tests/integration/antero-shadow-parity.integration.test.ts
>>>>>>> upstream/main
