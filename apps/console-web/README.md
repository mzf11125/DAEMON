# console-web

Next.js operational cockpit — signal inbox and case list via `@daemon/sdk-ts`.

```bash
pnpm --filter @daemon/console-web dev
```

Set `NEXT_PUBLIC_*_URL` in `.env` if services are not on localhost defaults.
