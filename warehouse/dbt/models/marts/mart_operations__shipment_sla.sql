-- Placeholder mart: on_time_delivery_rate inputs (Phase 5).
select
    cast(null as varchar) as shipment_id,
    cast(null as varchar) as ttk_number,
    cast(null as boolean) as on_time,
    current_timestamp as _as_of
where 1 = 0
