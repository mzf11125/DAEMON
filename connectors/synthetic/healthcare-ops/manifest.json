{
  "connectorId": "synthetic-healthcare-ops",
  "displayName": "Synthetic sandbox — healthcare-ops",
  "description": "Developer-local fixture connector; replay-only CSV/JSON under connectors/synthetic/healthcare-ops/",
  "version": "1.0.0",
  "paramsSchema": {
    "type": "object",
    "properties": {
      "fixturePath": { "type": "string", "default": "fixtures/sample.json" }
    }
  }
}
