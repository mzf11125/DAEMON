{
  "name": "@daemon/control-plane",
  "version": "0.1.0",
  "private": true,
  "description": "DAEMON control plane — tenant registry, health polling, metrics, and WebSocket log streaming",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "^5.3.2",
    "@fastify/websocket": "^11.0.2",
    "@fastify/cors": "^10.0.2",
    "ioredis": "^5.4.2",
    "zod": "^3.24.2",
    "pino": "^9.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3",
    "vitest": "^3.1.4"
  }
}
