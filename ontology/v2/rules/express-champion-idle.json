{
  "id": "express-champion-idle",
  "description": "Commercial account champion departure risk indicator",
  "sql": "SELECT observation_id, asset_id, value FROM dataset_observations WHERE label = 'express_champion_departure' AND value > {threshold:Float64}",
  "threshold": 0.5,
  "signalSeverity": "low"
}
