# Ontology language (schema bridge)

YAML/Zod schema source will align with vendored `packages/ontology-language` from daemon-system-ontology.

**Today:** canonical artifacts live under `ontology/v2/` in this repo.

```bash
make ontology-sync   # validate + future compile hook
make validate-ontology
```
